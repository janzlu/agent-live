# 🎙️ Agent Live 新手与日常使用指南（通俗易懂版）

> **一句话介绍**：Agent Live 是驻扎在您编辑器底部的**“语音军师”与“全自动伴飞副驾”**。  
> 您写代码时，不必反复切窗口打字，随手按一下快捷键即可像对讲机一样口头呼叫；AI 工程师在后台修改代码、跑测试、查错误时，它也会用字正腔圆的标准播音腔向您口头播报战况。

---

## 目录
1. [一分钟理解：它能帮我做什么？](#1-一分钟理解它能帮我做什么)
2. [极速上手：怎么用它来对讲？](#2-极速上手怎么用它来对讲)
3. [在 IDE 终端查看运行状态与战况看板](#3-在-ide-终端查看运行状态与战况看板)
4. [常用口头语音指令范例](#4-常用口头语音指令范例)
5. [日常终端命令小抄 (Cheatsheet)](#5-日常终端命令小抄-cheatsheet)
6. [三大贴心细节设计（无感不打扰）](#6-三大贴心细节设计无感不打扰)
7. [常见问题快速排查 (FAQ)](#7-常见问题快速排查-faq)

---

## 1. 一分钟理解：它能帮我做什么？

在平时用 Cursor 或 Antigravity IDE 写代码时，大家经常遇到两种累人场景：
1. **频繁打字很累**：想问个思路、查一下分支、或者让 AI 跑个单元测试，还要切到聊天框敲键盘打字。
2. **总要切窗口盯进度**：Agent 在后台自动化改代码、跑编译、做重构，你不知道它改到哪一步了，只能干等或者时不时切过去看日志。

**Agent Live 就是为了解决这两个痛点而设计的**：
- 🎙️ **对讲机式随叫随到**：按住/按一下全局热键 `Ctrl + Space`，直接开口说需求，说完它秒答。
- 🔊 **后台中立战报与风险把关（反“报喜不报忧”）**：任务结束拒绝盲目报捷粉饰，严格按照【变更实质、影响范围、遗留风险】三要素简炼汇报；平时具备 Red Team 批判性视角，主动提示架构与测试盲区。
- 🛑 **随时霸道打断 (Barge-in)**：AI 正在回复时，只要您再次按 `Ctrl + Space` 说话，AI 会在 0.1 毫秒内瞬间闭嘴并清空扬声器，绝对听长官指令。

---

## 2. 极速上手：怎么用它来对讲？

日常使用体验就像使用**对讲机 (Push-to-Talk)** 一样自然：

```text
       [ 键盘按下: Ctrl + Space ]
                   │
                   ▼
     ┌───────────────────────────┐
     │  🎙️ 麦克风开启 (能量条跳动) │  ◄── 您说话: "帮我看一下当前 Git 状态"
     └─────────────┬─────────────┘
                   │ 说话完毕: 再次按 Ctrl + Space (或停顿 1 秒)
                   ▼
     ┌───────────────────────────┐
     │  ⚙️ 引擎秒级理解 / 本地执行 │
     └─────────────┬─────────────┘
                   │
                   ▼
     ┌───────────────────────────┐
     │  🔊 扬声器清晰语音回复战报 │  ◄── "报告长官！当前在 main 分支，有 2 个文件待提交..."
     └───────────────────────────┘
```

### 极简操作步骤：
1. **呼叫开麦**：在电脑任意界面（无论在写代码、看网页还是在调试），按下 **`Ctrl + Space`**；
2. **开口讲话**：此时终端面板状态会变成绿色 `[🎙️ 开麦 ||||||]`，拾音能量条随您的声音跳动；
3. **结束发言**：说完后再次按 **`Ctrl + Space`**（或者说完自然停顿 1 秒），麦克风自动关闭；
4. **语音作答**：AI 立即通过扬声器向您口头汇报结果。

> 💡 **小贴士**：如果您当前光标正停留在 IDE 终端窗口内，直接按一下 **空格键** 或 **回车键** 也能瞬间完成开麦与闭麦！

---

## 3. 在 IDE 终端查看运行状态与战况看板

系统内置了全套状态监视与 HUD 战况看板，随时可查：

### ① 快速查看当前运行状态（推荐）
在 IDE 底部终端的**任何标签页**中输入：
```bash
agent-live status
```
终端会瞬间打印出漂亮的诊断仪表盘：
- **运行状态**：是否正在运行、进程 PID、占用内存与 CPU；
- **宿主与终端**：显示当前是 Antigravity IDE 还是 Cursor，正在哪个终端标签页 (`ttys015` 等) 挂载；
- **当前工程**：当前锚定的是哪个项目（例如 `xhmould`）；
- **设备与网络**：热键状态、声卡租约、代理状态。

### ② 动态监视看板（类似 htop 实时刷新）
如果想开着一个窗口时刻观察状态：
```bash
agent-live status -w
```
每 2 秒自动刷新一次，按 `Ctrl + C` 可随时退出。

### ③ 切换到现有的实时 HUD 终端面板
- 按快捷键 <kbd>⌃</kbd> + <kbd>`</kbd>（或 <kbd>⌘</kbd> + <kbd>J</kbd>）打开底部终端；
- 在终端右侧的标签列表中，找到运行 `start.sh` 或 `live_sidecar.py` 的那个标签页；
- 这里就是 **Tactical HUD（战况动态仪表盘）**，你会看到实时的拾音跳动能量条、对讲状态与伴随字幕。

### ④ 想把战况看板迁移到当前窗口？
如果您开了新的项目或新终端标签，想把看板移过来，直接输入：
```bash
agent-live restart
```
旧会话会自动安全释放，当前终端窗口将立刻变成专属战况看板！

### ⑤ 语音回复与战报军规（反“报喜不报忧”）
为确保耳朵收听最高效，语音顾问遵循以下严格表达军规：
1. **内容结构清晰，简洁明了**：层级分明、短句直奔要害，字数精炼，绝无虚头巴脑的口头禅与冗长铺垫。
2. **战报三要素中立平铺**：阶段任务结束时，按恰好 3 句短句（60~90字）中立通报：
   - **第1句【变更实质】**：客观直陈落地了什么核心改动或机制；
   - **第2句【影响范围】**：明确波及的模块边界与影响面；
   - **第3句【遗留风险】**：严苛点出代码隐患（如缺乏单测、未覆盖异常分支、未消 Warning 或待办），把评估裁量权交还长官。
3. **常态化 Red Team 参谋定位**：日常对讲与方案评审时，主动寻找架构盲区、安全隐患与边界缺失，直言利弊，严禁盲目迎合。

---

## 4. 常用口头语音指令范例

按开麦热键后，您可以随时像呼叫一位驻场中级工程师一样口头吩咐：

| 想要做的事情 | 您可以直接这样口头说 | 引擎动作 |
| :--- | :--- | :--- |
| **查代码仓库状态** | *“帮我看下当前 Git 分支和修改状态。”* | 自动执行 `git status` 并语音概括汇报 |
| **一键提交并部署官网** | *“提交并部署，更新官网。”* | 自动校验语法、提交推送 GitHub，并 SSH 登录 VPS 执行热更新部署！ |
| **跑工程自动化测试** | *“跑一下测试，看有没有报错用例。”* | 自动触发 `pnpm test` / `pytest`，播报测试结果 |
| **代码与项目梳理** | *“分析一下当前项目的目录架构和核心入口。”* | 结合当前工程目录结构快速口头梳理 |
| **查错与异常定位** | *“控制台刚报了个 500 错误，帮我分析下原因。”* | 梳理最新报错原因并口头给出排查建议 |
| **技术选型咨询** | *“我想给前端加个平滑滚动，有哪些推荐做法？”* | 给出业内通用方案与优缺点口头对比 |

---

## 5. 日常终端命令小抄 (Cheatsheet)

系统已配置了全局命令 `agent-live`，随时随地可用：

```bash
agent-live status       # 🔍 查看当前运行状态、健康度与绑定项目
agent-live status -w    # 📊 动态持续刷新监控 (按 Ctrl+C 退出)
agent-live restart      # 🔄 重启副驾并将实时战况 HUD 迁移至当前终端
agent-live stop         # 🛑 安全退出并关闭语音副驾 (写入防自愈 STOP 旗标)
agent-live logs         # 📜 实时追踪后台事件流水与对话日志
agent-live start        # 🚀 在当前终端直接前台启动
```

---

## 6. 三大贴心细节设计（无感不打扰）

很多开发者担心语音副驾会“太吵”或者“抢输入法”，Agent Live 做了极致优化：

1. **🔇 绝不干扰输入法（平时绝对闭麦）**  
   默认处于待命静音状态，绝不会监听你的日常办公室交谈，更不会打乱微信输入法或 Typeless 语音输入。只有当你主动按下 `Ctrl + Space` 时才会开始拾音。
2. **🎵 听歌开会智能避让（不乱插嘴）**  
   系统内置毫秒级音频嗅探机制。如果你正在用 Apple Music 听歌、看 B 站/YouTube 视频，或者正在开腾讯会议、飞书、Zoom 通话，**它绝不会冒然发声打扰**，而是将战报在后台安静排队；等您听完或会议挂断后，再从容向您汇报。
3. **🛡️ 喇叭麦克风硬件防自激（闭环消音）**  
   AI 在开口回复您的同时，会在毫秒内自动暂时关掉麦克风；当声音播放完毕后瞬间恢复。彻底消灭外放扬声器声音被麦克风二次录入造成的“回音尖叫与死循环”。

---

## 7. 常见问题快速排查 (FAQ)

### Q1: 按 `Ctrl + Space` 没有任何反应，麦克风不亮？
- **原因**：macOS 跨软件捕获全局快捷键需要系统“辅助功能”授权。
- **解决办法**：
  1. 打开 macOS **系统设置 -> 隐私与安全性 -> 辅助功能**；
  2. 确认列表中勾选了 **Antigravity IDE**（或 **Cursor** / **Terminal**）；
  3. 若刚勾选，重启一下软件即可生效。

### Q2: 听不到声音回复，但终端看到了文字？
- **原因**：可能由于系统音量过小，或者当前正在播放其他高优先级音频（触发了避让排队机制）。
- **解决办法**：检查系统扬声器输出设备是否正常，或者输入 `agent-live status` 查看“声卡播音租约”状态。

### Q3: 提示“无法连接 Gemini Live API”或“网络连接超时”？
- **原因**：Google Gemini 实时多模态接口在国内网络需要代理支持。
- **解决办法**：
  检查 `/Users/mon3xey/projects/agent-live/.env` 中的网络代理配置（如本地科学上网工具的端口）：
  ```env
  HTTP_PROXY=http://127.0.0.1:7897
  HTTPS_PROXY=http://127.0.0.1:7897
  ```

### Q4: 提示“通道单例互斥拦截，已有进程运行”？
- **原因**：为了防止两个终端同时开麦导致严重回音串音，系统严格限制同一 IDE 通道只能有一个前台实例。
- **解决办法**：直接运行 `agent-live restart`，旧实例会被平滑终止，并在当前终端重新拉起。
